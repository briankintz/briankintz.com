---
import BaseLayout from '../layouts/BaseLayout.astro'
import Highlight from '../components/Highlight.astro'
import LinkButton from '../components/LinkButton.astro'
---

<BaseLayout title="Page not found — Brian Kintz" description="This page doesn't exist.">
  <main class="wrap">
    <div class="hero">
      <p class="code">404 —</p>
      <h1>This page got <Highlight>torn out</Highlight>.</h1>
      <p>Whatever used to be here isn't in the notebook anymore — or never was. The homepage has everything that is.</p>
      <LinkButton class="cta" href="/" variant="primary">Back to the homepage</LinkButton>
    </div>
  </main>
</BaseLayout>

<style>
  .hero {
    padding: 96px 0 72px;
  }

  .code {
    font-family: var(--font-slab);
    font-size: 17px;
    font-weight: 600;
    color: var(--color-purple);
    margin-bottom: 16px;
  }

  h1 {
    font-family: var(--font-slab);
    font-size: var(--text-h1);
    font-weight: 700;
    line-height: 1.12;
    letter-spacing: -0.01em;
    max-width: 20ch;
  }

  .hero > p:not(.code) {
    margin-top: 22px;
    font-size: 17.5px;
    color: var(--color-soft);
    max-width: 54ch;
  }

  /* :global — the class lands on LinkButton's own root element, which carries
     LinkButton's scope id, not this page's, so a plain scoped selector won't match. */
  :global(.cta) {
    margin-top: 30px;
  }

  @media (max-width: 760px) {
    .hero {
      padding: 60px 0 48px;
    }
  }
</style>
