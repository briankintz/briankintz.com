---
// Highlighter swipe — at most once per page, on the hero heading (CLAUDE.md).
---

<span class="hl"><slot /></span>

<style>
  .hl {
    background: var(--pattern-highlight);
    padding: 0 2px;
  }
</style>
